create or replace
procedure sum(num1 is number, num2 in number,res out number)is
begin 
  res=num1+num2;
end;
/

> declare
sum1 number(10);
a number(5);
b number(5);
begin 
  a:=&a;
  b:=&b;
  sum(a,b,sum1);
  dbms_output.put_line('The sum of'||a||'and'|| b||'is'||sum1);
end;
/
